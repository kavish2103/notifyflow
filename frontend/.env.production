# Production environment variables for the Vite frontend build.
# Vite bakes these into the JS bundle at build time (import.meta.env.VITE_*).
# This file is committed to Git and is safe — it contains no secrets,
# only the public backend URL.

VITE_API_BASE_URL=http://localhost:3000
